from sympy.geometry import Line

class Cord:

    def __init__(self, line, length, intersection_points):
        self.line = line
        self.length = length
        self.intersection_points = intersection_points 
